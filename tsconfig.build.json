{
    "$schema": "https://json.schemastore.org/tsconfig",
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "target": "esnext",
        "module": "esnext",
        "jsx": "preserve",
        "moduleResolution": "bundler",
        "incremental": true // dts with tsup requires rootDir if incremental (which does not work well aliases)
    }
}
